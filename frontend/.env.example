# คีย์ API สำหรับ Vite dev proxy เท่านั้น (ใช้ฝั่งเซิร์ฟเวอร์ ไม่ถูกฝังลงใน bundle ของเบราว์เซอร์)
# ตั้งใจไม่ใช้ prefix VITE_ เพื่อป้องกันไม่ให้ Vite นำค่านี้ไป expose ใน client code
API_KEY=your-api-key-here
