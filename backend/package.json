{
  "name": "xbloom-dashboard-backend",
  "version": "1.0.0",
  "description": "Backend API สำหรับ xBloom Sales Dashboard (ดึงข้อมูลยอดขายจาก Google Sheets)",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --import ./test/setup.mjs --test"
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "express": "^4.19.0",
    "express-rate-limit": "^8.6.0",
    "ioredis": "^6.0.0"
  }
}
